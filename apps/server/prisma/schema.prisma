// Prisma schema placeholder.
// Tables: users, check_ins, persons, person_groups, important_dates,
//         shared_memories, stories, resonances, death_confirmations.
// Implement per packages/types definitions.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
