{
  "name": "@stillalive/web",
  "version": "0.0.1",
  "private": true,
  "description": "React Web 应用",
  "scripts": {
    "dev": "echo 'Web app — bootstrap pending'",
    "build": "echo 'Web app — bootstrap pending'",
    "typecheck": "echo 'Web app — bootstrap pending'",
    "clean": "rm -rf dist .next"
  },
  "dependencies": {
    "@stillalive/types": "workspace:*",
    "@stillalive/core": "workspace:*",
    "@stillalive/api": "workspace:*",
    "@stillalive/tokens": "workspace:*"
  }
}
