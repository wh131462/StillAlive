diff --git a/android/src/main/java/expo/modules/audio/AudioPlayer.kt b/android/src/main/java/expo/modules/audio/AudioPlayer.kt
index c76a9fd2a53743a691782e0f67d0d71401df58f6..e697a5f0461b8616801d7d1c6a3270310c757ced 100644
--- a/android/src/main/java/expo/modules/audio/AudioPlayer.kt
+++ b/android/src/main/java/expo/modules/audio/AudioPlayer.kt
@@ -24,6 +24,7 @@ import java.lang.ref.WeakReference
 
 private const val PLAYBACK_STATUS_UPDATE = "playbackStatusUpdate"
 private const val AUDIO_SAMPLE_UPDATE = "audioSampleUpdate"
+private const val REMOTE_CONTROL = "remoteControl"
 private const val SEEK_JUMP_INTERVAL_MS: Long = 10_000
 
 @UnstableApi
@@ -144,6 +145,10 @@ class AudioPlayer(
     }
   }
 
+  internal fun emitRemoteControl(action: String) {
+    emit(REMOTE_CONTROL, mapOf("action" to action))
+  }
+
   override fun onPlaybackStateUpdated(playbackState: Int, justFinished: Boolean) {
     val updateMap = mutableMapOf<String, Any?>(
       "playbackState" to playbackStateToString(playbackState)
diff --git a/android/src/main/java/expo/modules/audio/AudioRecords.kt b/android/src/main/java/expo/modules/audio/AudioRecords.kt
index 86880d92fd5b6413e8596e1b2b8ae28f2981addd..2cca326331f177748b49d4267d88635f26133497 100644
--- a/android/src/main/java/expo/modules/audio/AudioRecords.kt
+++ b/android/src/main/java/expo/modules/audio/AudioRecords.kt
@@ -110,6 +110,9 @@ enum class AndroidAudioEncoder(val value: String) : Enumerable {
 class AudioLockScreenOptions(
   @Field val showSeekForward: Boolean,
   @Field val showSeekBackward: Boolean,
+  @Field val showPrevious: Boolean = false,
+  @Field val showNext: Boolean = false,
+  @Field val showClose: Boolean = false,
   @Field val isLiveStream: Boolean? = null
 ) : Record
 
diff --git a/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt b/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt
index 196a5d57f887c2667a88543bd2f6a80044a044f2..a1c72358dac9edfc75c519e843d09cad95d6e22a 100644
--- a/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt
+++ b/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt
@@ -10,6 +10,8 @@ import android.media.AudioManager
 import android.content.pm.ServiceInfo
 import android.graphics.Bitmap
 import android.graphics.BitmapFactory
+import android.graphics.Color
+import android.net.Uri
 import android.os.Build
 import android.os.Bundle
 import android.os.IBinder
@@ -27,6 +29,7 @@ import expo.modules.audio.AudioLockScreenOptions
 import expo.modules.audio.AudioPlayer
 import expo.modules.audio.Metadata
 import expo.modules.audio.getPlaybackServiceErrorMessage
+import expo.modules.audio.R
 import expo.modules.kotlin.AppContext
 import kotlinx.coroutines.CoroutineScope
 import kotlinx.coroutines.Dispatchers
@@ -90,6 +93,9 @@ class AudioControlsService : MediaSessionService() {
 
         ACTION_SEEK_FORWARD -> currentPlayerRef.seekTo(currentPlayerRef.currentPosition + SEEK_INTERVAL_MS)
         ACTION_SEEK_BACKWARD -> currentPlayerRef.seekTo(currentPlayerRef.currentPosition - SEEK_INTERVAL_MS)
+        ACTION_PREVIOUS,
+        ACTION_NEXT,
+        ACTION_CLOSE -> currentPlayer?.emitRemoteControl(intent.action!!)
       }
     }
 
@@ -119,7 +125,7 @@ class AudioControlsService : MediaSessionService() {
 
   private fun buildPlaceholderNotification(): Notification =
     NotificationCompat.Builder(this, CHANNEL_ID)
-      .setSmallIcon(androidx.media3.session.R.drawable.media3_icon_circular_play)
+      .setSmallIcon(notificationIcon())
       .setContentTitle("‎")
       .setCategory(NotificationCompat.CATEGORY_TRANSPORT)
       .setSilent(true)
@@ -139,20 +145,23 @@ class AudioControlsService : MediaSessionService() {
   private fun createNotificationChannelIfNeeded() {
     val notificationManager = getSystemService(NOTIFICATION_SERVICE) as NotificationManager
     if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
-      if (notificationManager.getNotificationChannel(CHANNEL_ID) == null) {
-        notificationManager.createNotificationChannel(
-          NotificationChannel(
-            CHANNEL_ID,
-            CHANNEL_ID,
-            NotificationManager.IMPORTANCE_LOW
-          )
+      notificationManager.createNotificationChannel(
+        NotificationChannel(
+          CHANNEL_ID,
+          "音乐播放",
+          NotificationManager.IMPORTANCE_LOW
         )
-      }
+      )
     }
   }
 
   private fun buildContentIntent(): PendingIntent? {
-    val appIntent = packageManager.getLaunchIntentForPackage(packageName) ?: return null
+    val appIntent = packageManager.getLaunchIntentForPackage(packageName)?.apply {
+      action = Intent.ACTION_VIEW
+      val scheme = if (packageName.endsWith(".dev")) "stillalive-dev" else "stillalive"
+      data = Uri.parse("$scheme:///music-player")
+      addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP or Intent.FLAG_ACTIVITY_SINGLE_TOP)
+    } ?: return null
     return PendingIntent.getActivity(
       this,
       0,
@@ -175,7 +184,8 @@ class AudioControlsService : MediaSessionService() {
     val session = mediaSession ?: return null
 
     val builder = NotificationCompat.Builder(this, CHANNEL_ID)
-      .setSmallIcon(androidx.media3.session.R.drawable.media3_icon_circular_play)
+      .setSmallIcon(notificationIcon())
+      .setColor(Color.rgb(29, 107, 73))
       // If the title is null or empty string android sets the notification to "<AppName> is running..." we want to keep the notification empty so we \u200E to keep the text empty.
       .setContentTitle(currentMetadata?.title ?: "\u200E")
       .setContentText(currentMetadata?.artist)
@@ -183,21 +193,38 @@ class AudioControlsService : MediaSessionService() {
       .setLargeIcon(currentArtwork)
       .setContentIntent(buildContentIntent())
       .setAutoCancel(false)
+      .setOngoing(true)
+      .setOnlyAlertOnce(true)
+      .setSilent(true)
+      .setShowWhen(false)
+      .setPriority(NotificationCompat.PRIORITY_LOW)
+      .setVisibility(NotificationCompat.VISIBILITY_PUBLIC)
       .setCategory(NotificationCompat.CATEGORY_TRANSPORT)
 
     val style = MediaStyleNotificationHelper.MediaStyle(session)
 
-    // Older Android system UI expects explicit notification actions for transport controls.
-    if (Build.VERSION.SDK_INT <= Build.VERSION_CODES.S_V2) {
+    // Vendor System UI implementations may still require explicit transport actions on newer Android versions.
+    run {
       val compactViewIndices = mutableListOf<Int>()
       var currentIndex = 0
 
-      if (currentOptions?.showSeekBackward == true) {
+      buildContentIntent()?.let { openPlayerIntent ->
+        builder.addAction(
+          NotificationCompat.Action(
+            R.drawable.expo_audio_icon_open_player,
+            "打开播放器",
+            openPlayerIntent
+          )
+        )
+        currentIndex++
+      }
+
+      if (currentOptions?.showPrevious == true) {
         builder.addAction(
           NotificationCompat.Action(
-            androidx.media3.session.R.drawable.media3_icon_skip_back,
-            "Seek Backward",
-            buildActionPendingIntent(ACTION_SEEK_BACKWARD)
+            androidx.media3.session.R.drawable.media3_icon_previous,
+            "上一首",
+            buildActionPendingIntent(ACTION_PREVIOUS)
           )
         )
         compactViewIndices.add(currentIndex)
@@ -211,22 +238,33 @@ class AudioControlsService : MediaSessionService() {
           } else {
             androidx.media3.session.R.drawable.media3_icon_play
           },
-          if (session.player.isPlaying) "Pause" else "Play",
+          if (session.player.isPlaying) "暂停" else "播放",
           buildActionPendingIntent(if (session.player.isPlaying) ACTION_PAUSE else ACTION_PLAY)
         )
       )
       compactViewIndices.add(currentIndex)
       currentIndex++
 
-      if (currentOptions?.showSeekForward == true) {
+      if (currentOptions?.showNext == true) {
         builder.addAction(
           NotificationCompat.Action(
-            androidx.media3.session.R.drawable.media3_icon_skip_forward,
-            "Seek Forward",
-            buildActionPendingIntent(ACTION_SEEK_FORWARD)
+            androidx.media3.session.R.drawable.media3_icon_next,
+            "下一首",
+            buildActionPendingIntent(ACTION_NEXT)
           )
         )
         compactViewIndices.add(currentIndex)
+        currentIndex++
+      }
+
+      if (currentOptions?.showClose == true) {
+        builder.addAction(
+          NotificationCompat.Action(
+            R.drawable.expo_audio_icon_close,
+            "关闭",
+            buildActionPendingIntent(ACTION_CLOSE)
+          )
+        )
       }
 
       style.setShowActionsInCompactView(*compactViewIndices.toIntArray())
@@ -240,13 +278,12 @@ class AudioControlsService : MediaSessionService() {
     val session = mediaSession ?: return
     val mediaButtons = mutableListOf<CommandButton>()
 
-    // Add seek backward button if enabled
-    if (currentOptions?.showSeekBackward == true) {
+    if (currentOptions?.showPrevious == true) {
       mediaButtons.add(
-        CommandButton.Builder(CommandButton.ICON_SKIP_BACK_10)
-          .setDisplayName("Seek Backward")
+        CommandButton.Builder(CommandButton.ICON_PREVIOUS)
+          .setDisplayName("上一首")
           .setEnabled(true)
-          .setSessionCommand(SessionCommand(ACTION_SEEK_BACKWARD, Bundle.EMPTY))
+          .setSessionCommand(SessionCommand(ACTION_PREVIOUS, Bundle.EMPTY))
           .setSlots(CommandButton.SLOT_BACK)
           .build()
       )
@@ -255,25 +292,36 @@ class AudioControlsService : MediaSessionService() {
     // Add play/pause button (always present)
     mediaButtons.add(
       CommandButton.Builder(if (isPlaying) CommandButton.ICON_PAUSE else CommandButton.ICON_PLAY)
-        .setDisplayName(if (isPlaying) "Pause" else "Play")
+        .setDisplayName(if (isPlaying) "暂停" else "播放")
         .setEnabled(true)
         .setPlayerCommand(Player.COMMAND_PLAY_PAUSE)
         .setSlots(CommandButton.SLOT_CENTRAL)
         .build()
     )
 
-    // Add seek forward button if enabled
-    if (currentOptions?.showSeekForward == true) {
+    if (currentOptions?.showNext == true) {
       mediaButtons.add(
-        CommandButton.Builder(CommandButton.ICON_SKIP_FORWARD_10)
-          .setDisplayName("Seek Forward")
+        CommandButton.Builder(CommandButton.ICON_NEXT)
+          .setDisplayName("下一首")
           .setEnabled(true)
-          .setSessionCommand(SessionCommand(ACTION_SEEK_FORWARD, Bundle.EMPTY))
+          .setSessionCommand(SessionCommand(ACTION_NEXT, Bundle.EMPTY))
           .setSlots(CommandButton.SLOT_FORWARD)
           .build()
       )
     }
 
+    if (currentOptions?.showClose == true) {
+      mediaButtons.add(
+        CommandButton.Builder(CommandButton.ICON_UNDEFINED)
+          .setCustomIconResId(R.drawable.expo_audio_icon_close)
+          .setDisplayName("关闭")
+          .setEnabled(true)
+          .setSessionCommand(SessionCommand(ACTION_CLOSE, Bundle.EMPTY))
+          .setSlots(CommandButton.SLOT_FORWARD_SECONDARY)
+          .build()
+      )
+    }
+
     session.setCustomLayout(mediaButtons)
     session.setMediaButtonPreferences(mediaButtons)
   }
@@ -371,7 +419,7 @@ class AudioControlsService : MediaSessionService() {
           updateMetadata(metadata)
         }
         val session = MediaSession.Builder(context, sessionPlayer)
-          .setCallback(AudioMediaSessionCallback())
+          .setCallback(AudioMediaSessionCallback(::handleRemoteControl))
           .build()
 
         // Replace the basic media session with a session connected to our playback service.
@@ -452,7 +500,7 @@ class AudioControlsService : MediaSessionService() {
           updateMetadata(metadata)
         }
         val session = MediaSession.Builder(context, sessionPlayer)
-          .setCallback(AudioMediaSessionCallback())
+          .setCallback(AudioMediaSessionCallback(::handleRemoteControl))
           .build()
 
         player.mediaSession.release()
@@ -482,6 +530,17 @@ class AudioControlsService : MediaSessionService() {
     clearSessionInternal()
   }
 
+  private fun handleRemoteControl(action: String) {
+    appContext?.mainQueue?.launch {
+      currentPlayer?.emitRemoteControl(action)
+    }
+  }
+
+  private fun notificationIcon(): Int {
+    val identifier = resources.getIdentifier("notification_icon", "drawable", packageName)
+    return if (identifier != 0) identifier else androidx.media3.session.R.drawable.media3_icon_circular_play
+  }
+
   private fun loadArtworkFromUrl(url: URL, callback: (Bitmap?) -> Unit) {
     if (url != currentArtworkUrl) {
       currentArtworkUrl = url
@@ -558,6 +617,9 @@ class AudioControlsService : MediaSessionService() {
 
     const val ACTION_SEEK_FORWARD = "expo.modules.audio.action.SEEK_FORWARD"
     const val ACTION_SEEK_BACKWARD = "expo.modules.audio.action.SEEK_BACKWARD"
+    const val ACTION_PREVIOUS = "previous"
+    const val ACTION_NEXT = "next"
+    const val ACTION_CLOSE = "close"
 
     const val SEEK_INTERVAL_MS = 10000L
   }
diff --git a/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt b/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt
index 839aaef12d7699ea1a89037b31849775daa079f4..2aae612fe8f20c39c2cb9cf54ae5ddb3df5c5b15 100644
--- a/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt
+++ b/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt
@@ -8,9 +8,12 @@ import androidx.media3.session.SessionResult
 import androidx.annotation.OptIn
 import androidx.media3.common.util.UnstableApi
 import com.google.common.util.concurrent.ListenableFuture
+import com.google.common.util.concurrent.Futures
 
 @OptIn(UnstableApi::class)
-class AudioMediaSessionCallback : MediaSession.Callback {
+class AudioMediaSessionCallback(
+  private val onRemoteControl: (String) -> Unit = {}
+) : MediaSession.Callback {
   override fun onConnect(
     session: MediaSession,
     controller: MediaSession.ControllerInfo
@@ -35,6 +38,9 @@ class AudioMediaSessionCallback : MediaSession.Callback {
           MediaSession.ConnectionResult.DEFAULT_SESSION_COMMANDS.buildUpon()
             .add(SessionCommand(AudioControlsService.ACTION_SEEK_BACKWARD, Bundle.EMPTY))
             .add(SessionCommand(AudioControlsService.ACTION_SEEK_FORWARD, Bundle.EMPTY))
+            .add(SessionCommand(AudioControlsService.ACTION_PREVIOUS, Bundle.EMPTY))
+            .add(SessionCommand(AudioControlsService.ACTION_NEXT, Bundle.EMPTY))
+            .add(SessionCommand(AudioControlsService.ACTION_CLOSE, Bundle.EMPTY))
             .build()
         )
         .build()
@@ -49,14 +55,24 @@ class AudioMediaSessionCallback : MediaSession.Callback {
     command: SessionCommand,
     args: Bundle
   ): ListenableFuture<SessionResult> {
-    when (command.customAction) {
+    val handled = when (command.customAction) {
       AudioControlsService.ACTION_SEEK_FORWARD -> {
         session.player.seekTo(session.player.currentPosition + AudioControlsService.SEEK_INTERVAL_MS)
+        true
       }
       AudioControlsService.ACTION_SEEK_BACKWARD -> {
         session.player.seekTo(session.player.currentPosition - AudioControlsService.SEEK_INTERVAL_MS)
+        true
       }
+      AudioControlsService.ACTION_PREVIOUS,
+      AudioControlsService.ACTION_NEXT,
+      AudioControlsService.ACTION_CLOSE -> {
+        onRemoteControl(command.customAction)
+        true
+      }
+      else -> false
     }
+    if (handled) return Futures.immediateFuture(SessionResult(SessionResult.RESULT_SUCCESS))
     return super.onCustomCommand(session, controller, command, args)
   }
 }
diff --git a/android/src/main/res/drawable/expo_audio_icon_close.xml b/android/src/main/res/drawable/expo_audio_icon_close.xml
new file mode 100644
index 0000000000000000000000000000000000000000..03b38e781b88d9aec505f10657a987c697f1b617
--- /dev/null
+++ b/android/src/main/res/drawable/expo_audio_icon_close.xml
@@ -0,0 +1,9 @@
+<vector xmlns:android="http://schemas.android.com/apk/res/android"
+  android:width="24dp"
+  android:height="24dp"
+  android:viewportWidth="24"
+  android:viewportHeight="24">
+  <path
+    android:fillColor="#FFFFFFFF"
+    android:pathData="M6.4,5L12,10.6L17.6,5L19,6.4L13.4,12L19,17.6L17.6,19L12,13.4L6.4,19L5,17.6L10.6,12L5,6.4Z" />
+</vector>
diff --git a/android/src/main/res/drawable/expo_audio_icon_open_player.xml b/android/src/main/res/drawable/expo_audio_icon_open_player.xml
new file mode 100644
index 0000000000000000000000000000000000000000..2506fbfdc5842b7698cf648d04fcdef939826ade
--- /dev/null
+++ b/android/src/main/res/drawable/expo_audio_icon_open_player.xml
@@ -0,0 +1,9 @@
+<vector xmlns:android="http://schemas.android.com/apk/res/android"
+  android:width="24dp"
+  android:height="24dp"
+  android:viewportWidth="24"
+  android:viewportHeight="24">
+  <path
+    android:fillColor="#FFFFFFFF"
+    android:pathData="M8,11H14.17L11.59,8.41L13,7L18,12L13,17L11.59,15.59L14.17,13H8Z" />
+</vector>
diff --git a/build/AudioConstants.d.ts b/build/AudioConstants.d.ts
index cfd92ad0ff5765254c4e6db974fe44bce34aad06..f8d238925b435d6af657dc101de6628e185e482c 100644
--- a/build/AudioConstants.d.ts
+++ b/build/AudioConstants.d.ts
@@ -10,6 +10,18 @@ export type AudioLockScreenOptions = {
      * Whether the seek backward button should be displayed on the lock screen.
      */
     showSeekBackward?: boolean;
+    /**
+     * Whether the previous track button should be displayed in Android media controls.
+     */
+    showPrevious?: boolean;
+    /**
+     * Whether the next track button should be displayed in Android media controls.
+     */
+    showNext?: boolean;
+    /**
+     * Whether the close button should be displayed in Android media controls.
+     */
+    showClose?: boolean;
     /**
      * Whether the audio is a live stream. When `true`, the lock screen will hide the duration
      * and scrub bar, and disable seek controls.
diff --git a/build/AudioModule.types.d.ts b/build/AudioModule.types.d.ts
index f3ae4ee4044d86032cd5c6dbcfee6f905d1ce99f..5de497e017be6f0532064a4149e5591e81198ea1 100644
--- a/build/AudioModule.types.d.ts
+++ b/build/AudioModule.types.d.ts
@@ -222,6 +222,10 @@ export type AudioEvents = {
     playbackStatusUpdate(status: AudioStatus): void;
     /** Fired when audio sampling is enabled and new sample data is available. */
     audioSampleUpdate(data: AudioSample): void;
+    /** Fired when an Android media notification requests a queue action. */
+    remoteControl(data: {
+        action: 'previous' | 'next' | 'close';
+    }): void;
 };
 export declare class AudioRecorder extends SharedObject<RecordingEvents> {
     /**
diff --git a/ios/AudioRecords.swift b/ios/AudioRecords.swift
index 33db27c3b8fb40fa9db6659a0787c6b2b503d1ab..89f8b3f2882a9f7590750b572c5cc01f7fd7726f 100644
--- a/ios/AudioRecords.swift
+++ b/ios/AudioRecords.swift
@@ -69,6 +69,9 @@ struct Metadata: Record {
 struct LockScreenOptions: Record {
   @Field var showSeekForward: Bool = false
   @Field var showSeekBackward: Bool = false
+  @Field var showPrevious: Bool = false
+  @Field var showNext: Bool = false
+  @Field var showClose: Bool = false
   @Field var isLiveStream: Bool? = false
 }
 
diff --git a/src/AudioConstants.ts b/src/AudioConstants.ts
index dcc0452f7cae66b1bfaf77ed211416b092965bd9..0d8b018f2703006081d31ece9bb90e82d244d38b 100644
--- a/src/AudioConstants.ts
+++ b/src/AudioConstants.ts
@@ -11,6 +11,18 @@ export type AudioLockScreenOptions = {
    * Whether the seek backward button should be displayed on the lock screen.
    */
   showSeekBackward?: boolean;
+  /**
+   * Whether the previous track button should be displayed in Android media controls.
+   */
+  showPrevious?: boolean;
+  /**
+   * Whether the next track button should be displayed in Android media controls.
+   */
+  showNext?: boolean;
+  /**
+   * Whether the close button should be displayed in Android media controls.
+   */
+  showClose?: boolean;
   /**
    * Whether the audio is a live stream. When `true`, the lock screen will hide the duration
    * and scrub bar, and disable seek controls.
diff --git a/src/AudioModule.types.ts b/src/AudioModule.types.ts
index 7e7537173d067fecef767a03dc0f1860c8aca7ac..2583c6558b1e9f8d1e911f1fa98c5063939348bf 100644
--- a/src/AudioModule.types.ts
+++ b/src/AudioModule.types.ts
@@ -280,6 +280,8 @@ export type AudioEvents = {
   playbackStatusUpdate(status: AudioStatus): void;
   /** Fired when audio sampling is enabled and new sample data is available. */
   audioSampleUpdate(data: AudioSample): void;
+  /** Fired when an Android media notification requests a queue action. */
+  remoteControl(data: { action: 'previous' | 'next' | 'close' }): void;
 };
 
 export declare class AudioRecorder extends SharedObject<RecordingEvents> {
