diff --git a/lib/commonjs/utils/GestureHandler.js b/lib/commonjs/utils/GestureHandler.js
index 9d403dd7e69c2515036ec0d88eb4c16a6934d4b5..8e98be46560ab2e9b381866491ec28fa01fd0837 100644
--- a/lib/commonjs/utils/GestureHandler.js
+++ b/lib/commonjs/utils/GestureHandler.js
@@ -20,8 +20,8 @@ function GestureHandler({
   onLongPress,
   children
 }) {
-  const singleTap = _reactNativeGestureHandler.Gesture.Tap().runOnJS(true).maxDuration(250).onStart(onSingleTap);
-  const doubleTap = _reactNativeGestureHandler.Gesture.Tap().runOnJS(true).maxDuration(250).numberOfTaps(2).onStart(onDoubleTap);
+  const singleTap = _reactNativeGestureHandler.Gesture.Tap().runOnJS(true).maxDuration(250).maxDistance(6).onStart(onSingleTap);
+  const doubleTap = _reactNativeGestureHandler.Gesture.Tap().runOnJS(true).maxDuration(250).maxDistance(6).numberOfTaps(2).onStart(onDoubleTap);
   const longPress = _reactNativeGestureHandler.Gesture.LongPress().runOnJS(true).onStart(onLongPress);
   const swipeLeft = _reactNativeGestureHandler.Gesture.Fling().runOnJS(true).direction(_reactNative.I18nManager.isRTL ? _reactNativeGestureHandler.Directions.RIGHT : _reactNativeGestureHandler.Directions.LEFT).onStart(onSwipeLeft);
   const swipeRight = _reactNativeGestureHandler.Gesture.Fling().runOnJS(true).direction(_reactNative.I18nManager.isRTL ? _reactNativeGestureHandler.Directions.LEFT : _reactNativeGestureHandler.Directions.RIGHT).onStart(onSwipeRight);
@@ -64,7 +64,7 @@ function GestureHandler({
       flex: 1
     }
   }, /*#__PURE__*/_react.default.createElement(_reactNativeGestureHandler.GestureDetector, {
-    gesture: _reactNativeGestureHandler.Gesture.Exclusive(swipeLeft, swipeRight)
+    gesture: _reactNativeGestureHandler.Gesture.Exclusive(swipeLeft, swipeRight, doubleTap, singleTap)
   }, /*#__PURE__*/_react.default.createElement(_reactNative.View, {
     style: {
       width,
diff --git a/lib/module/utils/GestureHandler.js b/lib/module/utils/GestureHandler.js
index 58e8611af2178aaca51363f18a348929aeb8a9f0..1e94b4f5119ee2368116b411e9b93167c668fe91 100644
--- a/lib/module/utils/GestureHandler.js
+++ b/lib/module/utils/GestureHandler.js
@@ -13,8 +13,8 @@ export function GestureHandler({
   onLongPress,
   children
 }) {
-  const singleTap = Gesture.Tap().runOnJS(true).maxDuration(250).onStart(onSingleTap);
-  const doubleTap = Gesture.Tap().runOnJS(true).maxDuration(250).numberOfTaps(2).onStart(onDoubleTap);
+  const singleTap = Gesture.Tap().runOnJS(true).maxDuration(250).maxDistance(6).onStart(onSingleTap);
+  const doubleTap = Gesture.Tap().runOnJS(true).maxDuration(250).maxDistance(6).numberOfTaps(2).onStart(onDoubleTap);
   const longPress = Gesture.LongPress().runOnJS(true).onStart(onLongPress);
   const swipeLeft = Gesture.Fling().runOnJS(true).direction(I18nManager.isRTL ? Directions.RIGHT : Directions.LEFT).onStart(onSwipeLeft);
   const swipeRight = Gesture.Fling().runOnJS(true).direction(I18nManager.isRTL ? Directions.LEFT : Directions.RIGHT).onStart(onSwipeRight);
@@ -57,7 +57,7 @@ export function GestureHandler({
       flex: 1
     }
   }, /*#__PURE__*/React.createElement(GestureDetector, {
-    gesture: Gesture.Exclusive(swipeLeft, swipeRight)
+    gesture: Gesture.Exclusive(swipeLeft, swipeRight, doubleTap, singleTap)
   }, /*#__PURE__*/React.createElement(View, {
     style: {
       width,
