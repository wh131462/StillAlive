# ============================================
# StillAlive - 环境配置模板
# ============================================
# 使用说明:
#   开发环境: 复制此文件为 .env.development
#   生产环境: 复制此文件为 .env.production
# ============================================

# ==================== 通用配置 ====================

# 应用域名
DOMAIN=still-alive.me

# 环境 (development | production)
NODE_ENV=production

# ==================== 数据库配置 ====================

# PostgreSQL 数据库连接
POSTGRES_USER=stillalive
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=stillalive
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public

# ==================== 服务端配置 ====================

# JWT 密钥 (生产环境请使用强随机字符串)
JWT_SECRET=your_very_secure_jwt_secret_key_here_at_least_32_chars

# 服务端口
SERVER_PORT=4000

# ==================== 邮件配置 ====================

# SMTP 配置 (用于死亡确认邮件)
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your_email@qq.com
SMTP_PASS=your_smtp_authorization_code

# 是否启用死亡确认定时任务
ENABLE_DEATH_CONFIRMATION=true

# ==================== 前端配置 ====================

# API 地址 (前端访问后端的地址)
VITE_API_URL=https://api.still-alive.me

# ==================== 门户网站配置 ====================

# 下载链接
PORTAL_ANDROID_URL=https://still-alive.me/downloads/stillalive.apk
PORTAL_IOS_URL=https://apps.apple.com/app/stillalive
PORTAL_WEB_URL=https://app.still-alive.me

# GitHub 仓库地址
PORTAL_GITHUB_URL=https://github.com/your-username/stillalive

# ==================== Docker 配置 ====================

# 镜像版本标签
IMAGE_TAG=latest

# ==================== SSL 配置 ====================

# Let's Encrypt 邮箱 (用于 SSL 证书)
LETSENCRYPT_EMAIL=your_email@example.com

# ==================== 可选: 外部服务 ====================

# Claude API (用于年度总结 AI 生成)
# CLAUDE_API_KEY=sk-ant-xxxxx

# 新闻 API (用于每日信息差)
# NEWS_API_KEY=your_news_api_key
