## 1. 协议与加密基础

- [x] 1.1 复用现有 `@expo/dom-webview` 建立本机 Web Crypto 桥接并记录原因，确认不新增曲线依赖或 Portal 运行时远程依赖
- [x] 1.2 定义版本化邀请描述、响应明文和加密信封类型，以及 Base64URL、规范编码、字段白名单和固定字节上限
- [x] 1.3 实现移动端 P-256 密钥生成/ECDH、HKDF-SHA-256 和 AES-256-GCM 解密，并拒绝无效曲线点、未知版本及认证失败
- [x] 1.4 使用临时命令验证浏览器 Web Crypto 与移动端实现对公钥编码、共享密钥、HKDF、AAD 和 AES-GCM 的互操作结果一致，不新增测试文件

## 2. 待处理邀请存储

- [x] 2.1 增加 SQLite 迁移和 repository API，保存请求 ID、目标人物、字段范围、临时标签映射、过期时间及消费状态
- [x] 2.2 增加以请求 ID 为索引的 SecureStore 私钥保存、读取和删除逻辑，并在失败时补偿已创建的元数据
- [x] 2.3 实现过期邀请及私钥清理，确认待处理邀请和私钥未加入现有备份快照、导出与恢复流程

## 3. App 创建与分享邀请

- [x] 3.1 在人物详情增加“请对方补充资料”入口和字段选择界面，只提供姓名、性别、生日、MBTI 与已有自定义标签
- [x] 3.2 创建独立请求密钥对、7 天待处理记录和标签临时 ID 映射，生成不含 personId、当前字段值或私钥的邀请 fragment
- [x] 3.3 通过系统分享发送 HTTPS 邀请链接，并在创建或分享失败时清理本次未完成的请求和私钥

## 4. GitHub Pages 静态填写页面

- [x] 4.1 扩展 Portal 构建与页面路由，支持解析 `collect#request=` 和 `receive#response=`，且不把 fragment 内容写入请求、日志或持久存储
- [x] 4.2 实现邀请版本、过期时间、Web Crypto 能力和字段范围校验，并只渲染邀请允许的短文本/结构化字段
- [x] 4.3 使用浏览器 P-256 临时密钥、ECDH、HKDF-SHA-256 和 AES-256-GCM 生成受大小限制的加密响应信封
- [x] 4.4 实现响应 HTTPS 链接、系统分享、复制链接，以及接收页的“打开仍在”和复制响应码回退

## 5. App 解密与审核导入

- [x] 5.1 增加资料响应导入路由，统一识别可信 Portal HTTPS 链接、自定义协议参数和纯响应码，并在 App 内提供主动粘贴入口
- [x] 5.2 按请求状态、过期时间、人物存在性、信封大小和本地私钥严格验证并解密响应，再执行人物领域规则校验
- [x] 5.3 实现逐字段差异审核，允许排除单个变化，确保空答案不清除字段且生日响应不修改提醒偏好
- [x] 5.4 在一次保存流程中更新人物基础资料和标签；仅保存成功后消费请求并删除私钥，取消或失败时保持请求可用
- [x] 5.5 为未知、过期、已消费、人物已删除、私钥丢失、响应越权和认证失败提供不泄露明文的明确错误状态

## 6. 验证与交付

- [x] 6.1 运行 workspace typecheck、Portal typecheck/build 和移动端构建，确认未引入新的网络提交或服务器依赖
- [x] 6.2 手动验证篡改请求 ID、临时公钥、nonce、密文、标签选项和协议版本均被拒绝，重复响应在成功导入后不可再次使用
- [x] 6.3 在 iOS/Android 真机验证邀请分享、浏览器填写、响应回传、冷启动/热启动深链接和复制粘贴回退
- [x] 6.4 在真机验证完整允许字段下的链接长度，固化请求、响应和明文字节上限，并确认常用分享渠道不截断
- [x] 6.5 检查人物删除、邀请过期、SecureStore 私钥丢失和保存失败场景，确认不会误覆盖人物资料或错误消费请求
