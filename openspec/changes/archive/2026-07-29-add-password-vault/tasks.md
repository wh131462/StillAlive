## 1. 实施前安全与依赖门槛

- [x] 1.1 基于真实文档/包记录确认 Expo 57、React Native 0.86、iOS 和 Android 可用的维护中 AEAD 与内存困难 KDF 实现；记录算法、版本、nonce/盐长度、参数上下界和许可证，无法确认则停止实施
- [x] 1.2 确认 Expo 对应版本的系统安全存储、生物识别和剪贴板能力及真实 API 行为，只加入完成功能必需的最小依赖
- [ ] 1.3 在代表性 iOS/Android 真机测量 KDF 延迟和内存占用，固定版本 1 参数及导入允许范围，禁止普通 hash、自写加密或无界外部参数
- [ ] 1.4 复核 `ITSAppUsesNonExemptEncryption`、生物识别权限说明和商店发布声明，按实际集成结果更新平台配置，不沿用未经验证的当前值

## 2. 密码本领域模型、加密信封与本地存储

- [x] 2.1 新增应用内密码条目、版本 1 解密载荷和加密信封类型；所有用户字段只存在于加密载荷类型
- [x] 2.2 实现 CSPRNG 数据密钥生成、KDF 派生、DEK 包裹/解包、载荷 AEAD 加解密、关联数据域隔离和认证失败统一错误
- [x] 2.3 实现信封版本、大小、KDF 算法/参数上下界和 base64/二进制字段校验，拒绝未知版本、截断、篡改和资源耗尽输入
- [x] 2.4 实现独立 `vault.enc` 存储适配器，通过临时文件、回读验证和安全替换保留上一份有效密文；按真实文件 API 处理失败补偿
- [x] 2.5 实现密码本创建、加载、完整重写、主密码验证/修改和删除服务，不把条目加入 SQLite、settings 或 `BackupSnapshot`

## 3. 解锁会话与系统安全能力

- [x] 3.1 新增只作用于密码本路由的会话容器，持有解密载荷和短生命周期密钥，不接入全局 `AppStateProvider`
- [x] 3.2 实现首次主密码设置与确认、主密码解锁、错误/损坏统一提示和至少 6 字符校验，密码输入不得自动 trim 或改变大小写
- [x] 3.3 实现应用 `inactive/background`、离开模块、卸载和不可恢复错误时立即锁定、显示隐私遮罩、释放明文引用并尽力覆写密钥缓冲区
- [x] 3.4 实现可选指纹/面容快捷解锁的启用、使用、关闭和失效回退；系统安全存储仅保存受认证保护的 DEK，不保存主密码或条目
- [x] 3.5 实现主密码修改，要求再次验证当前主密码并只重新包裹 DEK；生物识别解锁不得绕过当前主密码验证

## 4. 密码本界面

- [x] 4.1 在「我的」页面增加密码本入口卡，不改变四个一级 Tab
- [x] 4.2 新增密码本路由布局、首次设置页、解锁页、空态和条目列表；导航参数只使用条目 ID
- [x] 4.3 新增条目创建/查看/编辑页，支持名称、账号、密码、网址和备注；名称与密码必填，密码默认遮蔽并关闭自动纠正/大写
- [x] 4.4 实现保存、编辑和删除确认，任何写入失败都保留上一份有效信封并显示不含敏感数据的错误
- [x] 4.5 新增密码本安全设置，提供生物识别开关、修改主密码和永久删除密码本入口
- [x] 4.6 实现锁定页“忘记主密码”说明与高强度删除确认，只删除密码本，不影响日记、人物和媒体

## 5. 显示、剪贴板与后台隐私

- [x] 5.1 实现用户主动显示/再次遮蔽密码；页面离开或会话锁定后恢复遮蔽
- [x] 5.2 实现显式复制和 60 秒清理状态；只有可靠确认仍为本次复制内容时才清理，不覆盖用户后来复制的内容
- [x] 5.3 检查密码本页面、导航参数、日志、错误、可访问性标签和崩溃诊断，确认不包含主密码、密钥或条目字段
- [ ] 5.4 在 iOS/Android 真机验证任务切换预览；若生命周期遮罩不足，只加入满足该要求所必需的平台能力并记录原因

## 6. 备份、恢复与全量删除

- [x] 6.1 扩展 ZIP 导出：密码本存在时原样加入 `vault.enc` 和 manifest 摘要，不存在时不生成；确认 `data.json`、Markdown、文件名和说明均无密码字段
- [x] 6.2 扩展备份解析：在任何 KDF 前校验 `vault.enc` 路径、大小、摘要、版本和参数范围，使用临时位置处理恢复
- [x] 6.3 实现密码本独立恢复：先验证备份主密码；当前密码本存在时再解锁当前密码本并单独确认，失败或取消保持原数据不变
- [x] 6.4 恢复旧备份或不含密码本的备份时默认保留当前密码本，避免现有覆盖恢复静默删除密码
- [x] 6.5 更新“删除全部本地数据”确认和执行流程，同时删除密码本正式/临时/回滚文件与系统安全存储材料

## 7. 文档、静态检查与真机验收

- [x] 7.1 更新 `docs/PRD_V3.md` 的「我的」、数据导出恢复、安全和阶段范围，文案明确主密码不可恢复且生物识别只是快捷解锁
- [x] 7.2 运行 workspace typecheck、lint（如项目存在对应脚本）和移动端静态构建，确认未改变日记、人物、媒体和现有备份语义
- [ ] 7.3 解压导出 ZIP 并搜索已知测试条目的名称、账号、密码、网址和备注，确认只存在通过认证加密保护的 `vault.enc`
- [ ] 7.4 在 iOS/Android 真机验证：首次设置、错误主密码、主密码修改、生物识别启停/失败、前后台锁定、模块离开、任务切换预览和剪贴板不覆盖新内容
- [ ] 7.5 在全新数据环境验证加密备份往返；验证旧备份保留当前密码本、错误备份不覆盖当前密文、恢复后需重新启用生物识别
- [ ] 7.6 验证单独删除密码本不影响日记，删除全部本地数据不遗留密码本或快捷解锁材料；不新建测试文件或示例文件
